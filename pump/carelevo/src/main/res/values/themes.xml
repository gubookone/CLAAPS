<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CareLevoAPS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CareLevoAPS" parent="Base.Theme.CareLevoAPS" />

    <style name="TopRoundBottomSheet" parent="@style/Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rect_top_round_16_white</item>
    </style>

    <style name="TopRoundBottomSheetDialog" parent="@style/Theme.MaterialComponents.DayNight.BottomSheetDialog">
        <item name="android:windowIsFloating">true</item>
        <item name="bottomSheetStyle">@style/TopRoundBottomSheet</item>
        <!--        <item name="android:statusBarColor">@color/color_white</item>-->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
        <item name="behavior_draggable">false</item>
    </style>

    <style name="TopRoundBottomSheetDialogTheme" parent="TopRoundBottomSheetDialog">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>